#include <iostream>
#include <vector>

using std::cout;
using std::endl;

int main()
{
    int hash[] = {1, 1, 2, 3};
    int hash1[] = {0, 0, 0};
    cout << (hash1 == 0) << endl;
    cout << (hash == 0)  << endl;
}
