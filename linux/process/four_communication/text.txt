1、四个进程，A进程和B进程负责通信，从标准输入读到的数据通过管道发送给对方，A1和B1进程负责显示
2、B进程从标准输入读到的数据通过管道发送给A后，A把数据放到共享内存里，A1从共享内存中拿到数据，打印到屏幕上
3、A进程从标准输入读到的数据发送给B后，B通过消息队列发送给B1，B1从消息队列中拿到消息，打印到屏幕上
退出方式：
通过Ctrl+c退出，任意一个进程收到Ctrl+c后，给所有进程发送一个10号信号，每个进程收到10号信号后，有序退出